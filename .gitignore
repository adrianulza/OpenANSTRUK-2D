# Dependencies
node_modules/

# Build output
dist/

# TypeScript cache
config/tsconfig.tsbuildinfo

# Environment variables
.env
.env.local
.env.*.local

# Logs
npm-debug.log*

# Local/personal files
.claude/
.vscode/
CLAUDE.md
CLAUDE-*.md
dev.local.mjs
vite.config.local.ts

# Solver cross-checks against commercial tools. Local-only: this repo is
# public, and the comparison harness is not something to publish.
validation/

# ⚠ Changed 2026-09-13 — three entries were removed from this file because the
# folders they named no longer exist here. Do not re-add them; look in the
# sibling repository instead.
#
#   src/3d/          -> d:\_Projects\OpenAnstruk-3D   (private)
#                       The 3D app was a NESTED repo hidden by a line here.
#                       Isolation is now STRUCTURAL: this build cannot reach
#                       source that is not in this tree. config/check-dist.mjs
#                       stays as a second layer, not the first.
#   marketing/       -> d:\_Projects\OpenAnstruk-Studio (private)
#   _complimentary/  -> split: brand assets to Studio, SAP2000 and rc_design
#                       reference to the 3D repo.

# TypeScript incremental build cache — regenerated by `tsc -b` on every build.
tsconfig.tsbuildinfo
