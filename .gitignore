# Dependencies
node_modules/

# Build output
dist/

# TypeScript cache
config/tsconfig.tsbuildinfo

# Environment variables
.env
.env.local
.env.*.local

# Logs
npm-debug.log* 

# Local/personal files
.claude/
.vscode/
CLAUDE.md
dev.local.mjs
vite.config.local.ts
_complimentary/
validation/
marketing/

# OpenAnstruk-3D — local-only source. Its own nested git repo with no remote.
# Never remove this line; never `git add -f` anything beneath it.
src/3d/

# TypeScript incremental build cache — regenerated by `tsc -b` on every build.
tsconfig.tsbuildinfo
